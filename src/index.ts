// This code exports a function createClient that allows you to easily create a new instance of the DeepgramClient with the provided API key and options. It encapsulates the process of constructing a client instance for interacting with the Deepgram API.

export const createClient = (
  apiKey: string,
  options?: DeepgramClientOptions
): DeepgramClient => {
  // Create and return a new instance of the DeepgramClient with the provided API key and options
};
